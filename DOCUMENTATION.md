---
name: material-approval-pipeline
triggers: ["材料報批", "材料審批", "材料審批表", "材料報批表", "工程材料審批文件", "材料審批總表", "BQ合併", "報批表加BQ頁", "型號提取", "Material Submittal", "材料報批包", "品牌推薦", "材料品牌"]
description: 工程「材料報批文件包」全流程製作管線（v2.9）。觸發詞見上。當任務係為工程（政府 / 私人 / 學校 / 酒店）投標產出一套完整材料審批包（總表→分項報批表→材料PDF含BQ頁→型號列→品牌填寫）時用。本技能係材料報批嘅唯一權威來源，已內化：5步鐵律（含用戶確認關卡）、引擎選擇決策樹、編號規則速查、分批提交+回復雙文件夾工作流、物料替換（同等物料）流程、BQ頁合併（merge_bq.py，支援自動+圖像型+跨頁+產品資料+多BQ分隔符+點號BQ+修訂尾綴+AR/AG/EG/EL/ELV/FS/AC/3P/M多前綴）、多專業前綴（AC/AR/AG/EG/EL/ELV/FS/M）同BQ章節(A/B/C/D/E)對應、雙承建商（總包+分判）報批、品牌推薦引擎（實戰補充後 414 條）。Excel 生成細節可參考 xlsx-template-batch-fill。
version: 2.9.0
icon: 📋
metadata:
  clawdbot:
    requires:
      bins:
        - python
        - excel
---

# 工程材料報批文件包 · 全流程管線 v2.9

> 本版本（2026-08-05）深度內化《材料审批完整工作流程 v1.0》（項目負責人 編，項目負責人 確認）嘅方法論，並將「BQ 頁合併」由項目寫死嘅 `append_bq.py` 升級為通用、CLI 驅動、支援自動+圖像型+跨頁嘅 `merge_bq.py`，同時將 162 條黃金清單品牌推薦引擎一併併入，令技能自包含、唔再跨環境依賴。
>
> **v2.1.0（2026-08-06）**：由某環保項目 陳年工程實戰材料審批文件學習補強——① 品牌庫 +19 條實戰品牌（FLOTEX/KNAUF/CORIAN/OMEXCO/BRAVAT/VITRA/Belden 等），總數 162→289；② 編號規則加「BQ前綴=專業大類」「修訂尾綴全集」「合同編號 RCL-DSPA4F」「中文序號前綴（土建XX）」「申報→批復→雙簽流程」；③ `merge_bq.py` 支援總表「多 BQ 編號（、分隔）」自動拆分全部相關頁。
>
> **v2.2.0（2026-08-06）**：由某大學-某大學一樓裝修工程（2025）實戰材料審批文件學習補強——① **關鍵修正**：BQ 編號前綴（B=建築/D=機電 EME/E=電力）係**項目/顧問特定、唔係固定規則**，必須由總表/BQ PDF 讀取，唔可硬編碼；② 新編號格式：AR-xx（建築2位）、EL-0XX-A/B（修訂遞進）、ELV-00X-A（多模/單模光纖變體）、AC-0001（冷氣4位零填）、EL-0XXB(Ln)（燈具B版+燈型括號，含無dash及嵌空格括號變體）；③ 加某大學/酒店雙語模板結構（Ref.No + Item Contratual + 勾選框 + {JBF} 佔位符）同可複製自動鏈（源表格.xlsm → 逐編號生成 → merge_bq）；④ 品牌庫 +9 條某大學實戰品牌（DIMON/Campton至高/UKOO照明/Gerflor/LAMBERT/PORTINO/Scoretech/Synthomer/ITC），總數 289→298。
>
> **v2.3.0（2026-08-06）**：由承建商聯營體-承建商聯營體-某政府學院住宿樓裝修工程材料审批V3.0（2023）實戰學習補強——① 業主專屬編號體系 `3P2023-MTL-{DR/EL/ELV/PL}-[4位]` 完整化（含跳號 `3P203`、MST 施工方案前綴變體；並記低 `DR` 前綴多義：某政府學院=排水，某大學/某政府項目=建築裝飾）；② BQ 編號格式加 `E.1.2.1`（點號）+ 括號補充「間接包括 X.XXX-X.XXX 項」；③ 修訂尾綴全集加弱電 `-N` / 強電 `_NN` / DR `_01` / `(業工程部 reply)`/`(顧問單位 reply)` 回復標記；④ 審批流程加某政府學院雙簽 `■用家`+`■工務局` 及回復檔「原設計 vs 報批」對比結構；⑤ 模板佔位符系統（`XH`/`CLMC`/`BQXM`/`SBRQ`/`MTL`/`MST` 純大寫，不同於某大學 `{JBF}`；勾選框用私用區字符）；⑥ 品牌庫 +22 條某政府學院實戰品牌（FG Wilson/Honeywell/H3C/Dsppa/Furse/XAL/NEMO/FLOS 等），總數 298→320。
>
> **v2.4.0（2026-08-06）**：由承建商-利馬-某市政項目維修及更改工程（市政部門，工程編號 某市政項目）材料審批文件夾深度學習補強——① **雙編碼系統**：表格編號 `EL-XXX`（內部，如 EL-093）≠ 合約 BQ 編號（純數字 `1.1.1`/`4.13`，欄位 `BQ項目編號Item Contratual`）→ `merge_bq.py` 必須用 `--code-map`（EL→BQ）對應，因 PDF 文件名 `EL-XXX` 唔等於 BQ 碼；② **市政部門材料審批表**（中葡雙語）固定欄位：R8=表格編號(EL-XXX)、R11=報批之材料、R15=牌子/Marca、R22=BQ項目編號、R19=數量+供貨日期、附件欄=樣板/目錄/來源證/測試結果——生成時對齊呢啲 cell；③ **修訂尾綴新樣式**：無括號字母（`EL-063D`/`EL-081A`/`EL-134A`）、`(3.0)` 版本前綴（`(3.0)EL-135A`）、`（取消）`取消標記、`-副本`副本標記；④ **BQ 浮點偽影**：由 xlsx 讀 BQ 碼會變 `4.1800000001` → 提取時要 `float→%g` 清理；⑤ **品牌大小寫不一致**：`MK`/`Mk`/`mk` 同義 → `brand_recommender.py` 應做 case-insensitive 匹配+歸一化；⑥ **「無」品牌**：表示無指定品牌（如聚酯纖維箱）；⑦ 品牌庫 +42 條某市政項目實戰品牌（Hoye/Legrand/MAS/Nexans/MK/Rovasi/Unitech/KDK/toto/berlin德國寶/PLI/PIL/ESSE-CI/WEW/UUNIECH/JSL/HALOPRISM/ARES/燈下科技/Hi-lu*/非常時光/Uniech/Furse 等），總數 320→362。
>
> **v2.5.0（2026-08-06）**：由某學校（分校）地面層入口大堂、過道、一樓衛生間更改工程（2025，學校項目）材料審批文件夾深度學習補強——① **新編號前綴 `AR-XX`**（Approval Request，學校/裝修項目常用，唔係 EL-/B/D/E/3P），與某市政項目 `EL-`、某政府學院 `3P2023-` 並列為第三類前綴；② **材料碼嵌喺名稱**（防水膜 W-01、白色防滑地磚 F-01、牆身磚 BR-01、乳膠漆 PL-01、實木門 D4/5/6、不鏽鋼龍頭 CL-01、洗手池 ST-02、軟包 CT-01）→ PDF 文件名 = `AR-01 防水膜W01.pdf`；③ **BQ 碼格式唔一致**（同項目內 `D1.2` 無點 vs `D.1.3` 有點、`J4.1` vs `J.3.1`、`M1`/`M2` 無點）→ 比對前要正規化（統一加/去點、補前導零）；④ **分批提交工作流**：`材料審批-20250708/09/12/14/0812` 五個日期文件夾，對應總表「申報時間」欄（YYYYMMDD）→ 提交按批次歸檔；⑤ **回復雙文件夾**：`材料審批回復/工程承建商材料報批表`（我方提交件，AR01.pdf 等，image-based 掃描件）+ `材料審批回復/*.pdf`（業主/顧問批復件）；⑥ **物料替換/同等物料流程**：`re/sika替換材料` 放 Gripset/Denka vs Sika 對比 PDF（SikaTop 300 / Sika Latex / Estokote Flexi），即「指定品牌 → 申請同等物料」嘅替代論證；⑦ **`帶品牌.pdf` 單頁品牌速查表**（標書品牌一覽，OCR 有水漬：`乳賿漆`=乳膠漆、`軟⻑鏡`=軟長鏡、品牌位出 `3/6/2/5` 亂碼=圖片logo未OCR→需人工補）；⑧ 修訂重編號：`AR-033`(不鏽鋼尿池)=主表`AR-36`、`AR-035`=主表`AR-39`（修訂版重排序號）；⑨ `decl=與設計相同` 慣例（AR-05~18 大量出現，表示沿用設計品牌）；⑩ 品牌庫 +14 條（駿華/NIRO GRANTE 瓷磚、Nippon 立邦、SHUERDA 不鏽鋼、SOKOWELL 座廁、富美家/箭牌/TOTO 新規格項），總數 362→376。
>
> **v2.6.0（2026-08-06）**：由市政部門-某公廁項目工程-某公廁項目一樓（工程編號 **某公廁項目**，注意文件夾名 `0107` 係錯寫，真實編號係 `某公廁項目編號`）材料審批文件夾深度學習補強——① **一個項目跨多專業前綴（最大新規律）**：單一 xls 同時含 `AC`(冷氣/Air-Con,5) + `AR`(建築裝修/Architectural,41) + `EG`(電氣一般/Electrical-General 設備,4) + `EL`(電力/Electrical,28) + `FS`(消防/Fire-Safety,4) 五類前綴，共 82 項 → 技能必須支援**同一份總表多前綴並存**，唔可以假設單一前綴；② **內部碼前綴 ↔ BQ 章節對應**：`AR`→`C`章節(建築 C2.2.1)、`EG/EL`→`E`章節(電氣 E1.2/E6.2)、`FS`→`F`章節(消防 F1.1)、`EG`部分→`D`章節(給排水 D1.1) → 前綴只係內部分類，BQ 碼章節先係合約真章節；③ **BQ 碼直接做文件夾名**：`E.1.2 電箱OP1-3N`、`D.1.1 tub-e`、`D.2.1.1 KEY EN877` → 材料 catalog PDF 按 BQ 碼歸檔，`merge_bq.py` 搜 BQ 頁時要喺呢啲文件夾搵；④ **雙承建商報批**：主包 `工程承建商` + 分判 `分判承建商` 各自交一份 市政部門材料審批表（分判承建商版 `分判承建商材料表-20200720.xls`，sheet 名帶 `（A）` 修訂尾綴如 `EL-028（A） 時間掣`）→ 處理時要區分承建商，唔好當同一份；⑤ **回復文件夾雙層結構**：`材料審批回復-第一次回復-20201222/` 含「合併回復 `X-材料審批回復.pdf`」(AC/AR/EG/EL/FS 各一份) + 「個別官方批復 `內部批復_000174.pdf`/`內部批復_000178.pdf` 等」(內部批復 = 市政部門內部批復編號，掃描件零文字層) → 我方提交後業主蓋章回來；⑥ **三向簽核**：主表「申報批復」欄有 `■ 則師`+`■ 用家`+`■ 工務局` 三個 checkbox（比某政府學院雙簽更完整）；材料審批表尾部簽名位 `承建公司 / 質監單位 / 監管公司` + `意見/簽名`；⑦ **內部資料/分判承建商參考** = 帶 BQ 碼嘅材料 catalogue PDF（Schneider Acti9 iC60/iSW、TYT、ARDEX EG-8、KEY EN877、Iplex UPVC、Formica 廁格）＋ 分判商材料表 → 呢啲係產品資料來源，BQ 頁合併時優先揀；⑧ 品牌庫 +27 條（miTEC 緊急呼叫、BODE/ALBA 瓷磚收邊、德高 Davco 瓷磚膠、PBS 扶手皂液盅、ALCOM 自動門、BiLock/CENTRIC/WKW 門鎖五金、AUTO SOURCE LED緊急照明、FORMICA 鋁蜂窩門/廁格、Esse-ci 燈具、TOTO+Viyya 洗手盤等），總數 376→403。
>
> **v2.7.0（2026-08-06）**：由承建商-某社會設施項目-某社會設施項目-為某社會設施項目社會設施進行優化工程（承建商，學校/社會設施項目）材料審批文件夾深度學習補強——① **全新 `M-XX` 弱電通訊前綴**（M1~M46，覆蓋 42U機櫃/電話總機/緊急呼叫系統/門禁電鎖/拉繩呼叫按鈕，前 6 項目未見，與 `EL-`/`ELV-` 平行共存）→ 技能多前綴表要加 `M`；② **分層 BQ 編碼** `2E.17.1`/`2H.11.1~2H.11.31`（章節 `2E`電力/`2H`給排水衞生 + 子章節字母 + 子序號，`2H.11.1~31` 係評估室1~12等批量同類房間）→ 比對前正規化；③ **4 批提交模式**（強電 `EL-` 第1~2批、弱電 `M` 第3~4批，混合 `ELV-`/`M` 兩套前綴）；④ **報價 xlsx 文件名含 NBSP `\xa0` 不可見字符** + 全/半角括號 → 開檔要用 `glob.glob('*機櫃*材料表*.xlsx')` 模糊匹配，唔可以硬拼路徑；⑤ **品牌跨拼寫變體**（入庫歸一化）：`CommScope`/`Comscope`、`PANASONIC`/`panisonic`→`Panasonic`、`Austin`→`AUSTIN-HUGHES`；⑥ 品牌庫 +10 條（BOSCH 緊急呼叫 IP7400XI / CommScope 線纜 / IFCS 呼叫按鈕 / Prysmian 3×2.5mm²電線 / 佛山照明 T8 LED燈盤 / AMP Cat6 / AUSTIN-HUGHES 42U機櫃 / Panasonic 幹手機+抽氣扇），總數 403→413。

> **v2.7.0（2026-08-06）**：由某社會設施項目-某社會設施項目社會設施優化工程（某社會設施項目）實戰內化——① **全新 `M-XX` 弱電通訊前綴**（M1~M46，機櫃/電話總機/緊急呼叫/門禁/拉繩呼叫按鈕，前 6 項目未見），與 `EL-`/`ELV-` 平行共存；② **分層 BQ 編碼** `2E.17.1`/`2H.11.1~2H.11.31`（章節+字母後綴+子序號，同類房間批量）；③ **4 批提交模式**（強電 EL 第1~2批、弱電 M 第3~4批）；④ **報價 xlsx 文件名含 NBSP `\xa0` 不可見字符 + 全/半角括號** → 開檔要用 `glob` 模糊匹配；⑤ **品牌跨拼寫變體**（`CommScope`/`Comscope`、`PANASONIC`/`panisonic`→`Panasonic`、`Austin`→`AUSTIN-HUGHES`，入庫歸一化）；⑥ 品牌庫 +10 條（BOSCH/CommScope/IFCS/Prysmian/佛山照明/AMP/AUSTIN-HUGHES/Panasonic規格），總數 403→413。
>
> **v2.8.0（2026-08-06）**：由某政府大樓項目-某政府大樓項目諮詢-某政府大樓項目辦公空間裝修工程（政府辦公大樓）實戰內化——① **完整 7 前綴系統** `AR`/`AG`(供水)/`EG`(排水)/`AC`/`FS`/`EL`/`ELV`（比 某公廁項目 多 `AG`+`ELV` 分拆；`EG` 前綴多義：某政府大樓項目=排水，某公廁項目=電氣一般）；② **BQ 章節字母映射**（某政府大樓項目 `A`建築/`B`供排水/`C`冷氣/`D`消防/`E`強弱電）→ 章節字母含義因項目而異，必須由總表讀（`A`=建築喺某政府大樓項目、`B`=建築喺某政府項目/某大學）；③ **「跟原大廈牌子」慣例**（消防 FS 沿用原大廈 Viking/Killfire/EDWARDS）、**區域內容表**新文檔類型（區域→天花/牆身/地面/傢私+BQ）、**三層工作流**（設計選定→待批核→按專業歸檔）+ 多版本總表；④ **雙語品牌 cell**（中文\n英文同一牌子：耐克森/Nexans、康普/Commscope、愛思創/Extron、華為/HUAWEI）、**BQ 字母/數字混淆**（`C.l.l`=`C.1.1`、點號不一致 `C5.1` vs `C.3.1`）；⑤ 品牌庫 +1 條（IKEA/宜家 傢俬）；其餘 ~30 品牌（Viking/Killfire/EDWARDS/Daikin/Nexans/Selperfect/Moonstar/Armaflex/Owens Corning/Formica/興友騰/MexyTech/Extron/華為/CommScope/Draka/WEW/Crown/Trust/Schneider…）**已全部覆蓋** → 知識庫品牌廣度獲實證驗證；總數 413→414。
> **v2.9.0（2026-08-26）**：由某學校中學部暑期校舍工程（2026-08-05 增量加料實戰）內化 + 自我複查機制固化——① **某學校模板風格四確認**：per-item 報批表 BQ = 第 5 行 `G5:L5`(標籤)+`M5:Q5`(值)，**糾正技能舊假設 `H5`**（照舊假設填會令 BQ 顯示唔到 / 隱藏值殘留）；總表 12 欄 A~L、隔行底色 zebra、標題合併 `A1:J1`；② **路徑現實紅線**：某學校真源 = `材料文件\`（非 `材料審批\`），舊腳本路徑要手改指向；③ **3 個新數據坑**：win32com 寫 Python `datetime` → 日期 off-by-one（要寫 raw serial）、BQ 格位唔可以信舊假設（生成前先 fingerprint known-good 行）、loop render 確認浪費（用 `page.get_text()` 一次驗證）；④ **一鍵加料工作流**：新增 `add_materials.py`（pre-flight→設值→出 PDF→append 產品資料→sync 總表→sync 美化版）+ `helpers.py`（`excel_serial`/`set_bq_cell`/`fingerprint_bq`），取代 6 支散腳本；⑤ **自我複查纪律 SOP**（每次任務後 5 步：量化差距→歸納根因→固化落技能→下次試跑確認），根因 = 47 分鐘任務本可 5 分鐘搞掂（靠估+試錯 vs 讀模板一次→重用）。知識已全部同步至 `<KB_DIR>/材料審批技能/`（v2.9 / 9 項目 / 9 腳本）。

## 呢個技能解決咩

用戶（承建商，如工程承建商 / 承建商聯營體）會俾一份「材料統計匯總表」+ Excel 報批表範本（+ 工程標書 PDF），要你產出一整套**可以提交嘅材料審批文件包**：
1. **材料審批總表**（12 欄，全部 item 編好號、填好品牌/貨期/型號）
2. **N 份分項材料報批表**（xlsx，每項一份，保留範本 logo/字體）
3. **N 份材料 PDF**（每份 = 審批表 + 工程標書 BQ 頁 + 產品資料，按正確順序）
4. **品牌欄自動填寫**（無標書指定品牌時，用 413 條黃金清單推薦）

---

## 🔴 核心鐵律：5 步流程（含用戶確認關卡）

> 來源：2026-06-18 項目負責人 親自確立「材料審批 5 步流程紅線」。每步完成**必須向用戶匯報結果 → 等用戶確認 → 先執行下一步**。跳步係最慘痛教訓（2026-06-17 某體育館因跳步導致極差體驗）。

| Step | 階段 | AI 操作 | 關卡 |
|:---|:---|:---|:---|
| 1 | **掃描版 PDF → 清單** | 用 pdf 工具抽取標書/報價單全文（無文字層先 OCR/渲染），輸出純文字材料清單（名稱/數量/規格/品牌要求） | — |
| 2 | **分析 → 提出報批清單** | 由清單識別材料、歸類 5 大類、分配 EL/ELV 編號、對應 BQ 編號 | — |
| 3 | **征詢用戶同意** | 展示清單 + 明確提問「以上分析是否正確？」 | ⛔ **必須等用戶確認，禁止跳步** |
| 4 | **製作材料審批總表** | 按引擎決策樹生成 總表.xlsx（12 欄） | — |
| 5 | **用戶確認總表 → 批量生成** | 用戶確認無誤 → 批量生成報批表 xlsx → 導出 PDF → 合併 BQ 頁 →（可選）導出最終 PDF | ⛔ **總表確認後先批量，禁止未確認就爆量生成** |

**Step 3 標準話術**：
```
用戶，我已分析完成：
📊 報批清單：共 xx 項材料
- 強電 EL-001 ~ EL-xxx
- 弱電 ELV-001 ~ ELV-xxx
- 分類：一.電箱 x 項 / 三.電線 x 項 / 五.其它 x 項
📋 [展示表格：編號/材料/BQ編號/品牌/數量/分類]
以上分析是否正確？確認後我開始製作總表。
```

**Step 5 標準話術**：展示總表路徑 + 抽查 1 份報批表，問「總表內容無誤？確認後我批量生成 + 合併 BQ」。

---

## 🧠 引擎選擇決策樹

> 每次 Step 4 生成總表/分項報批表前，必須按模板特徵自動判斷用邊個引擎。

```
            ┌── 模板含 logo/中文字體/合併格？ ──┐
            ↓ 是                                ↓ 否（純文字/數據）
   ┌── 有 Windows + Excel？ ──┐            openpyxl 方案
   ↓ 是                      ↓ 否          （gen_biao.py / xlsx-template-batch-fill）
 win32com 整張 .Copy    純 ZIP 方案
 （gen_approval_forms.py） （dem-material-approval gen.py 思路）
            └── 需要 BQ 合併？ ──┘
                     ↓
              merge_bq.py
```

| 場景 | 用邊個引擎 | 原因 | 本技能對應腳本 |
|:---|:---|:---|:---|
| 模板含 **logo / 中文字體 / 合併格**（最常見，如某學校/學校報批表） | **win32com 整張 Copy** | openpyxl clone 會搞亂中文字體+過大 logo；ZIP 要手寫 XML 映射 | `scripts/gen_approval_forms.py` |
| 含圖片/打印設置、但**無 Excel 環境**或要批量多 Sheet 高保真 | **純 ZIP 方案** | 繞過 openpyxl，保留所有二進制資源 | 思路見 dem-material-approval；本技能用 win32com 替代 |
| **純文字/數據模板**（無圖片，字段坐標靈活） | **openpyxl** | 靈活適配任意字段，自動讀列寬 | `xlsx-template-batch-fill` / `gen_biao.py` 思路 |
| 唔肯定模板類型 | 先診斷 | `inspect` 睇佔位符/圖片/合併格 | 手動開範本睇 |

**點解唔用 openpyxl 操作含圖片模板？** `copy_worksheet()` + `save()` 會丟 `printerSettings.bin`、丟 DrawingML 圖片、破壞 rId 映射。→ 含圖片一律 win32com 整張 Copy 或純 ZIP。

---

## 🔢 編號規則速查

> 每次報批前查呢段，避免編號錯亂。**2026-08-06 由某環保項目 陳年工程實戰補強**（BQ前綴=大類、修訂尾綴全集、合同編號、雙簽回復流程）。

### 5 大功能分類法（某市政項目體系，用於 BQ 歸組）
| 分類 | 包含內容 | 示例 |
|:---|:---|:---|
| **一. 電箱部份** | 配電箱、電箱元件、斷路器、MCB、RCCB、接觸器 | QP3.1、RX3 MCB、DX3 RCCB |
| **二. 接地部份** | 接地極、接地線、接地端子、防雷材料 | 接地極銅棒、接地線 25mm² |
| **三. 電線部份** | 電纜、電線、母線槽 | V 4x10、XV 2x2.5 |
| **四. 灯具部份** | 照明燈具、應急燈、指示牌 | LED平板燈、出口指示燈 |
| **五. 其它** | 插座、開關、線管、橋架、輔材 | 13A插座、PVC管 |

### 編號前綴（本技能用，跟用戶確認過嘅硬規則）
建築 **AR-001**、機電 **EL-001**、供水 **AG-001**、排水 **EG-001**、冷氣 **AC-001**。泥水/木器/潔具→AR；機電→EL；供水喉/銅喉→AG；地漏→EG。
> 🆕 **實戰補充（某政府項目2020）**：業主項目常用**中文前綴序號**如「土建01~土建67」（建築）、「機電XX」；冷氣用「MAT-BS-AC-01~32」；給排水用「EG-004」；弱電/回復用**合同主導編號**「RCL-DSPA4F-2020xxx」（顧問/業主批復追蹤號，非材料序號，出現喺回復文件命名）。

### 強電 / 弱電 格式
| 格式 | 示例 | 說明 |
|:---|:---|:---|
| `EL-[序號]` | EL-027 | 單個材料/電箱 |
| `EL-[序號]-[子]` | EL-029-1 ~ EL-029-23 | 同系列電箱分組 |
| `EL-[序號][字母]` | EL-034A / EL-062B | 同一物料多版本修訂 |
| `ELV-[序號]` | ELV-001 ~ ELV-011 | 弱電 |
| `ELV-[序號]-R1/R2` | ELV-007-R1 | 業主修訂輪次 |
| `ELE-[序號]` | ELE-03 | 機電混合（某媒體項目） |

### 🆕 某大學-某大學實戰編號格式（2026-08-06 補強）
| 格式 | 示例 | 說明 |
|:---|:---|:---|
| `AR-[2位]` | AR-01 ~ AR-67 | 建築序號（2 位，唔零填） |
| `EL-[3位]-A/B` | EL-017A、EL-017B、EL-019A、EL-020A | 同一物料多輪修訂（A→B 遞進） |
| `ELV-[3位]-A` | ELV-002A、ELV-003A | 弱電變體（多模 A=單模光纖版） |
| `AC-[4位零填]` | AC-0001 ~ AC-0026 | 冷氣/通風空調（**4 位零填**，唔係 AC-001） |
| `EL-[3位]B(Ln)` | EL-035B(L1) ~ EL-056B(L12)、EL-053B(L6地面層) | 燈具 B 版 + 括號燈型代號 |
| `EL0XXB` 無 dash | EL044B(L10) | 燈具 B 版偶爾缺 dash（EL044B ≠ EL-044B） |
| `EL-0XXB(L13 凹燈箱 (地面層))` | EL-047B(L13 凹燈箱 (地面層)) | 括號內可嵌空格/中文/二級括號（文件名解析要容忍） |
> ⚠️ 修訂版燈具實戰會擺喺**獨立子文件夾**（如 `强电文件/B版燈具審批/`），處理時要連同子文件夾一齊掃。AC 冷氣用 4 位零填（AC-0001），同其它項目嘅 AC-001 唔同——以總表序號列為準。

### 🆕 某學校分校實戰編號格式（2026-08-06 補強，學校/裝修項目）
| 格式 | 示例 | 說明 |
|:---|:---|:---|
| `AR-[2~3位]` | AR-01 ~ AR-47、AR-033、AR-035 | **Approval Request 前綴**（學校/裝修工程常用，第三類前綴，與某市政項目 `EL-`、某政府學院 `3P2023-` 並列） |
| 材料碼嵌名稱 | `AR-01 防水膜W01.pdf`、白色防滑地磚 F-01、牆身磚 BR-01、乳膠漆 PL-01、實木門 D4/5/6、不鏽鋼龍頭 CL-01、洗手池 ST-02、軟包 CT-01 | **材料碼（2字母±數字）嵌喺材料名**，PDF 文件名 = `AR-XX 名+碼.pdf`；睇名即可知類別（W=防水、F=地磚、BR=牆磚、PL=漆、D=門、CL=潔具、ST=石/池、CT=軟包） |
| 修訂重編號 | `AR-033`(不鏽鋼尿池)=主表`AR-36`、`AR-035`=主表`AR-39` | 修訂版重新排序號，**文件名序號 ≠ 主表序號**，要以主表 `材料跟蹤表總表` 序號列為準做映射 |
| `decl=與設計相同` | AR-05~18 大量出現 | 申報品牌沿用設計指定，唔另填品牌（唔等於「無品牌」） |

> ⚠️ **BQ 碼格式唔一致（某學校實戰）**：同一項目內 BQ 前綴有點無點混用——`D1.2`（無點）vs `D.1.3`（有點）、`J4.1` vs `J.3.1`、`M1`/`M2`（無點）、`N1.1`/`N2.1`（有點）。`merge_bq.py` 比對 BQ 頁前**必須正規化**：統一補/去前導字母後嘅點、補前導零（如 `J4.1`→`J.4.1`）。唔好假設同項目 BQ 格式統一。

### 🆕 多專業前綴並存 + 雙承建商（市政部門某公廁項目 某公廁項目 實戰，2026-08-06 補強）
| 格式 | 示例 | 說明 |
|:---|:---|:---|
| 單項目多前綴 | `AC-`(冷氣) / `AR-`(建築裝修) / `EG-`(電氣一般設備) / `EL-`(電力) / `FS-`(消防) | **同一份總表/同一工程可同時有 5 類前綴**，共 82 項（某公廁項目 實戰：AC5+AR41+EG4+EL28+FS4）。技能唔可以假設單一前綴，要掃晒全部前綴 |
| 前綴 ↔ BQ 章節對應 | `AR`→`C`章節、`EG/EL`→`E`章節、`FS`→`F`章節、`EG`部分→`D`章節(給排水) | **前綴只係內部分類代號，BQ 碼章節（C/D/E/F）先係合約真章節**。表格編號 `AR-001` 對應 BQ `C2.2.1`，`EL-001` 對應 `E1.2` → 內部碼前綴同 BQ 章節無固定 1:1 |
| BQ 碼做文件夾名 | `E.1.2 電箱OP1-3N`、`D.1.1 tub-e Copper`、`D.2.1.1 KEY EN877`、`E.1.2(1) Acti9 iSW` | 材料 catalogue PDF 按 **BQ 碼 + 設備名** 歸檔（無獨立 EL-XXX 文件名）。`merge_bq.py` 搜 BQ 頁要喺呢類「BQ 碼文件夾」搵對應 PDF |
| 雙承建商報批 | 主包`工程承建商` + 分判`分判承建商` | 同一工程兩間公司各交一份 市政部門材料審批表。**分判版 sheet 名帶 `（A）` 修訂尾綴**（如 `分判承建商材料表` 中 `EL-028（A） 時間掣`、`EL-001（A） 電箱OP2-5N`）→ 處理時要區分承建商，唔好當同一份重複 |
| 回復雙層結構 | `材料審批回復-第一次回復-20201222/`：合併 `AC/AR/EG/EL/FS-材料審批回復.pdf` + 個別 `內部批復_000174.pdf`/`內部批復_000178.pdf` | **內部批復_xxxxxx.pdf** = 市政部門內部批復編號（掃描件零文字層）；合併回復按專業分卷。我方提交（工程承建商/分判承建商 材料審批表）→ 業主蓋章回來 |
| 三向簽核 | 主表「申報批復」欄 `■ 則師`+`■ 用家`+`■ 工務局` | 比某政府學院雙簽（`■用家`+`■工務局`）多咗 `■ 則師`。材料審批表尾部簽名位：`承建公司 / 質監單位 / 監管公司` + `意見/簽名` |
| 內部資料/分判承建商參考 | `內部資料/`（TYT、Schneider Acti9 iC60/iSW、ARDEX EG-8、KEY EN877、Iplex UPVC、Formica 廁格）+ `分判承建商參考/分判承建商材料表.xls` | 帶 BQ 碼嘅產品 catalogue PDF 係 BQ 頁合併優先來源；分判商材料表提供分判商視角規格 |

> ⚠️ **工程編號錯寫陷阱（某公廁項目 實戰）**：文件夾名寫 `0107-2020-DEM`，但 xls 內文工程編號係 **`某公廁項目`**（真正編號）。引用工程編號時要以 xls/圖則為準，唔好抄文件夾名。

### 🆕 分批提交 + 回復 + 物料替換 工作流（某學校實戰）
| 環節 | 文件夾/檔名 | 說明 |
|:---|:---|:---|
| 分批提交 | `材料審批-20250708` / `...-20250709` / `...-20250712` / `...-20250714` / `...-20250812` | 按**申報日期**分 5 批歸檔，對應總表「申報時間」欄（YYYYMMDD 格式，如 `20250708`）→ 提交時按批次建文件夾 |
| 我方提交件 | `材料審批回復/工程承建商材料報批表/AR01.pdf`（等，image-based 掃描件，5頁無文字層） | 最終報批表（市政部門材料審批 格式，表格編號 Ref.No + 合約項目編號 Item 雙碼），係 `merge_bq.py` 嘅 manual 模式處理對象 |
| 業主/顧問回復 | `材料審批回復/20250715105123042.pdf`、`...-20250805141957899.pdf` | 批復件（含蓋章/簽名掃描） |
| 物料替換/同等物料 | `re/sika替換材料/`（Gripset 11y & Sika Latex、Gripset 2P & SikaTop 300 seal、Denka Estokote Flexi & SikaTop 300 seal） | 申請「指定品牌 → 同等物料」替代時，放**對比論證 PDF**（原品牌 vs 替代品牌嘅技術對照），證明同等或更優 |
| 品牌速查表 | `某學校-材料審批表-帶品牌.pdf`（單頁，標書品牌一覽） | 無文字層表格嘅速查來源；OCR 有水漬：`乳賿漆`=乳膠漆、`軟⻑鏡`=軟長鏡、品牌位出 `3/6/2/5`=圖片logo未OCR→需人工補品牌 |
| 修訂版總表 | `某學校-材料審批表-r1.xls`（1.9MB，含 `瓷磚數量`/`潔具` 細分 sheet） | `-r1` = revision 1；修訂版 `標書品牌` 欄已填滿（40 項），並加細分數量 sheet → 優先讀修訂版攞品牌 |
| 主表格式 | `.xls`（舊格式，需 `xlrd`/`pandas` 讀；`openpyxl` 唔支援） | 讀 `.xls` 用 `pd.ExcelFile(engine='xlrd')`；sheet `材料跟蹤表總表` 序號喺第 5 行起（header 佔 4 行） |

### 🆕 修訂尾綴全集（實戰觀察，2026-08-06 補強）
| 尾綴格式 | 示例 | 來源/含義 |
|:---|:---|:---|
| `(A)/(B)/(C)` | 土建03（A）、EL-001（B） | 全角括號修訂輪次（最常用） |
| `(1)/(2)/(3)` | 土建19(1)、土建28(1)(A) | 子項修訂（括號內數字） |
| `R1/R2/R3` | 消音器20200818R1、風機控制箱出街檔R3 | 修訂輪次（R = Revision） |
| `REV1/REV2` | MAT-BS-AC-31 REV1 | 版本修訂 |
| `02A` | MAT-BS-AC-02A | 字母尾綴（替代/修訂版） |
| `出街檔R3` | 風機控制箱 出街檔R3 | 送審版標記（非最終） |
| `日期+R` | 20200818R1 | 日期戳 + 修訂輪次 |
| `弱電 -N` | ELV-2401-2、-3 | 減號+數字輪次（某政府學院2023 弱電修訂） |
| `強電 _NN` | EL-2188_04、EL-2190_03 | 底線+雙位數輪次（某政府學院2023 強電修訂） |
| `DR _01` | DR-1827_01 | 底線+雙位（某政府學院2023 排水修訂） |
| `(業工程部 reply)` / `(顧問單位 reply)` | 3P2023-MTL-EL-2137-01 (業工程部 reply) | 業主(業工程部)/顧問(顧問單位)回復檔標記（某政府學院2023） |
| `字母尾綴(無括號)` | EL-063D、EL-081A、EL-134A | **某市政項目**：修訂版用單字母尾綴，唔包全角括號（D=A版後再修、A/B/C 遞進） |
| `(3.0) 版本前綴` | (3.0)EL-135A | **某市政項目**：括號內版本號 + 字母尾綴（防水80A 4極隔離開關連箱） |
| `（取消）` | EL-018-QSC-1F-1.3（取消） | **某市政項目**：該申報項取消，唔再審批（文件名解析要識別並跳過/標記） |
| `-副本` | EL-130-8寸窗口式抽氣扇 - 副本 | **某市政項目**：相同材料另存副本（文件名解析要忽略 `-副本` 後綴，映射返原 EL 碼） |

**修訂尾綴優先級**：無 → A → B → C → AA（最完整版本）。
> ⚠️ 文件名尾綴（如 `(B)`）與 PDF 內文可能**不同步**（實戰見某政府項目 `EL-001（A）` 文件名寫 `(B)`、內文係 `(A)`）—— 以**最終簽署版 / 業主回復**為準，唔好盲目跟文件名。

**歷史最大值**：某市政項目 **EL-151**（最大編號工程）；新工程由 **EL-001** 起。

### 🆕 BQ 編號前綴 = 項目/顧問特定（⚠️ 關鍵修正：唔係固定規則！）
> **2026-08-06 某大學-某大學實戰重大修正**：BQ 前綴**因項目/顧問而異，絕對唔可以硬編碼**！已觀察到三種：
> - 某政府項目2020：`E` = 電力（E1.1.1 配電箱）
> - 某大學-某大學：`B` = 建築（B3.1.2、B4.2）、`D` = 機電 EME（D1.1.1~D1.1.19 配電箱/電纜）
> - 某市政項目/一般：純數字（1.1.1、2.1.4）
> - **某政府學院2023（承建商聯營體）**：`E.1.1.1` / `E.1.2.1`（**點號分隔**，非 E1.1.1），且常附「（間接包括 1.3.1-1.3.14 項）」括號補充多 BQ → `merge_bq.py` 需識別點號格式 + 括號內多編號
>
> ⚠️ **`DR` 前綴多義**：同一個 `DR`，喺某大學/某政府項目 = 建築裝飾（DoR），喺某政府學院2023 = 排水/Drainage（`MTL-DR排水`）—— **含義因項目而異，務必對照總表 sheet 名（MTL-PL供水 / MTL-DR排水）**。
>
> ⚠️ **BQ 浮點偽影（某市政項目實戰）**：由 xlsx 讀取純數字 BQ 碼（如 `4.13`）時，openpyxl 常返 `4.1800000001` 呢類 float 偽影 → 提取/比對時必須 `float(v)` → `"%g" % v` 清理成 `4.18`。`merge_bq.py` 嘅 BQ 比對正則要容忍呢種浮點，或先 normalize。`build_bq_text_index` 已用 `\b([A-Za-z]?\d+\.\d+(?:[-.]\d+)?)\b`，純數字 `4.18` 可命中，但輸入碼要先清理浮點。
>
> **正確做法**：永遠由**總表 BQ 欄** 或 **BQ PDF 文字層** 讀取真實前綴，唔好假設。`merge_bq.py` 已支援任意單字母前綴（B/D/E…）、純數字、及點號格式（E.1.2.1）。

| BQ 前綴 | 專業大類（觀察到的項目） | 示例 |
|:---|:---|:---|
| `B` | 建築/Building | B2.2、B3.1.2、B3.2.1、B4.2、B9.1 |
| `D` | 機電/EME（某大學-某大學） | D1.1.1~D1.1.19 配電箱/電纜 |
| `E` | 電力/Electrical（某政府項目2020） | E1.1.1 |
| 純數字 | 通用/依總表 | 1.1.1、2.1.4 |
> ⚠️ 一個材料可對應**多個 BQ 編號**（如土建08 BQ = `B3.3.2、B3.3.4、B3.3.5、B3.3.6、B3.3.7`，用`、`分隔）→ `merge_bq.py` 已支援自動拆分全部相關頁（唔會漏）。

### 電箱組件字母排序（固定，每個電箱文件夾內）
`A.` MCB｜`B.` RCCB｜`C.` Fuse carrier｜`D.` Fuse｜`E.` DRX/MCCB｜`F.` Contactor/Interlock｜`G.` Isolating Switch/SircoM｜`H.` IDEC YW｜`I.` Panel 箱體｜`J.` Controller/Power supply｜`K.` BUSBAR/端子排

### 業主專屬格式（例：某政府學院住宿樓 承建商聯營體，2023-12-07）
完整體系：`3P2023-MTL-{DR/EL/ELV/PL}-[4位]`（DR=排水、EL=強電、ELV=弱電、PL=供水；均接 `MTL-` 材料前綴）。
| 專業 | 前綴 | 示例 |
|:---|:---|:---|
| 強電 | `3P2023-MTL-EL-` | EL-2101 Q.G.B.T、EL-2188_04 |
| 弱電 | `3P2023-MTL-ELV-` | ELV-2401、ELV-2401-2（減號輪次）、ELV-2426_2（底線輪次） |
| 供水 | `3P2023-MTL-PL-` | PL-1501、PL-1520水泵 |
| 排水 | `3P2023-MTL-DR-` | DR-1801、DR-1827_01 |

**⚠️ 實戰變體（唔可硬編碼！）**：
- **跳號**：`3P203-MTL-PL-1520水泵.pdf`（2023→203 縮寫、且缺中間 `-MTL-` 分隔）→ 文件名唔一定完全規則，merge 時靠 `--code-map` 對應總表序號。
- **MST 前綴**：`3P2023-MST-0013_02`（MST=施工方案，與 MTL 材料並行）→ 提交分「材料(MTL) / 施工方案(MST) / 其他」三類。

**🆕 模板佔位符系統（某政府學院2023 實戰）**：模板.xlsx 用**純大寫欄位名**（非某大學 `{JBF}` 花括號風格），生成時按項目替換：
`XH`=序號 ｜ `CLMC`=材料名稱 ｜ `BQXM`=BQ項目編號 ｜ `SBRQ`=申報日期 ｜ `MTL`=材料 ｜ `MST`=施工方案
> ⚠️ **勾選框用私用區字符**（PDF 中 `\uf052`/`\uf0a3` = ☑/☐ 渲染）—— 唔好當亂碼刪除，生成 PDF 時保留。
> ⚠️ **每個項目/顧問模板佔位符慣例不同**（某大學 `{JBF}` vs 某政府學院 `XH/CLMC`），`gen_approval_forms.py` 必須**讀取該項目模板提取佔位符**，唔可假設統一格式。

### 🆕 業主審批流程：申報 → 批復 → 雙簽（實戰，某政府項目2020 / 某政府學院2023）
總表唔止申報，仲含**回復/批復**欄位，最終要**雙簽確認**：
- **申報狀況**：申報品牌 / 申報時間 / 產地 / 型號 / 材料確認（■ 樣板）
- **申報批復**：業主/顧問寫「YYYY/MM/DD 可以接受」或「不可以接受，需提交詳細資料，包括型號，產品介紹等」
- **雙簽確認**：
  - 某政府項目2020：`■ 則師` + `■ 用家`
  - **某政府學院2023**：`■ 用家`（業主 某政府學院）+ `■ 工務局`（政府 工務部門/工務部門）—— 見材料跟蹤表總表 R4 雙簽欄
- **回復檔結構**（某政府學院2023 `材料報審（回複）/`）：`(業工程部 reply)`=業主回復、`(顧問單位 reply)`=顧問回復；內含「**原設計之機械/材料** vs **報批所提供之機械/材料**」雙欄對比（牌子/產地/型號/供貨期），合 BQ 頁時呢類回復檔應附於審批表之後。
- **所屬編號**：標記材料歸屬邊批回復（如土建06(A) 所屬=A），對應多輪回復批次
> 呢段對應 Step 5「總表確認」——**業主回復後先算定稿**，批量生成前必確認批復欄已填、雙簽位預留。

### 🆕 某大學-某大學 報批表模板結構（2026-08-06 學習）
實戰見 `FaceTemple.xlsx` / `源表格.xlsm` / `2025-10-28_1.xlsx`，某大學/酒店項目用**雙語模板**，結構如下（我方 `gen_approval_forms.py` 生成時要對齊）：
- 標題：`某大學-某大學一樓裝修工程`
- `表格編號 / Ref. No.`：填材料編號（如 EL-051）
- `R`：修訂標記位（右側）
- `合約中之項目編號 Item Contratual：`：填 BQ 編號
- `報批之材料：`：材料名稱
- 類別勾選框：`土木 Civil` / `建築 Arqt.` / `機械 EME`（三選一打 ■）
- **佔位符自動化**：模板用 `{JBF}` 代表編號，再由腳本逐編號替換生成（實戰有 `FillExcelTemplate` 流程：讀 `源表格.xlsm` → 備份 → 逐 `Ref. No.` 生成 `2025-10-28_1.xlsx` 多 sheet）。我方 `gen_approval_forms.py` 已係同一思路，可直接承接 `源表格.xlsm`（編號/物料/品牌/BQ/數量/貨期/日期 7 欄）做輸入。

### 🆕 自動化流程模式（實戰驗證可抄）
某大學項目已驗證一條**可複製自動鏈**：
```
源表格.xlsm（權威數據：編號/物料/品牌/BQ/數量/貨期/日期）
   ↓ 讀取（openpyxl）
FaceTemple.xlsx（雙語模板 + {JBF} 佔位符）
   ↓ 逐編號替換生成
2025-10-28_1.xlsx（每編號一 sheet）
   ↓ 逐 sheet 匯出 PDF
各編號 PDF（再 merge_bq.py 合 BQ 頁 + 產品資料）
   ↓ 回填
材料跟蹤表總表.xlsx（序號/名稱/BQ/品牌/型號/數量/單位/貨期/區域 + 申報頁數/批復/雙簽）
```
> 關鍵：`源表格.xlsm` 係最可靠嘅 BQ 映射來源（某大學 D1.1.1~D1.1.19 全部喺度），優先於由 PDF 文件名推。我方 Step 0 應優先讀 `源表格`/`總表` 拎 BQ 映射。

### 🆕 某市政項目-維修更改工程（市政部門）實戰模式（2026-08-06 深度學習）
工程編號 `某市政項目`，承建商承建商，業主**市政部門**。文件夾 `（自動批處理）材料審批表` 含 `1.材料審批表強電弱電+材料跟蹤表.xlsm`（總表）、`2.模板.xlsx`（「模板」sheet 53×38）、`3.材料審批表.xlsx`（每 sheet = 一件材料，命名 `EL-XXX-設備名`）、分類子文件夾（一.電箱/二.接地/三.電線/四.燈具/五.其它）、`材料資料`。

**🔑 雙編碼系統（最關鍵）**：
- **表格編號（內部）** = `EL-XXX`（序號欄，如 EL-093）→ 同 PDF 文件名前綴一致
- **BQ項目編號（合約）** = 純數字點號（`1.1.1`、`4.13`、`11.8`）→ 填喺審批表 R22 `BQ項目編號Item Contratual`
- ⚠️ 兩者**完全唔同**！但 `merge_bq.py` **自然支援**：佢由總表同時讀「序號(col1→配對 input PDF `EL-XXX`)」同「BQ(col3→喺 BQ PDF 搜尋 `4.13`)」，所以雙編碼自動處理，**唔使 code-map**。`--code-map` 只係當「總表序號 ≠ PDF 文件名」時先要用（如某政府學院跳號 `3P203`）。某市政項目 BQ 欄（純數字）已經係現成映射，優先由度讀。

**🏛️ 市政部門材料審批表（中葡雙語）固定欄位**（生成 `3.材料審批表.xlsx` 時對齊）：
| 欄位 | Cell | 內容 |
|:---|:---|:---|
| 業主抬頭 | R1 | 市政部門 |
| 工程編號 | R3 | 某市政項目-某市政項目維修及更改工程 |
| 表格編號 Ref.No. | R8 (col 8) | `EL-093`；col 9 = `XH` |
| 報批之材料 Material Proposto | R11 (col 7) | 設備名（如 LED光管盤） |
| 牌子 Marca | R15 (col 7) | 實際申報品牌（如 Hoye） |
| 數量 Quantidade / 供貨日期 | R19 | 數量 + `90日` |
| BQ項目編號 Item Contratual | R22 (col 11) | 純數字 BQ（如 4.13） |
| 附件 Anexos | R26-28 | 樣板/目錄/來源證/測試結果（勾選） |
| 雙簽 | R31/R33/R36/R39 | 承建單位 + 設計單位 + 顧問單位 + 市政部門 |

**🔁 per-sheet 審批表工作流**（某市政項目「自動批處理」模式）：
```
2.模板.xlsx（「模板」sheet，53×38）
   ↓ 每項 wsTemplate.Copy（win32com 整張）
3.材料審批表.xlsx（每 EL-XXX 一件 = 一 sheet，命名 EL-XXX-設備名）
   ↓ 逐 sheet 填值（R8/R11/R15/R19/R22）
   ↓ 逐 sheet 匯出 PDF
各 EL-XXX PDF + 對應材料 PDF（EL-XXX-名.pdf）組合提交
```
> 同某大學 `源表格.xlsm→{JBF}替換`、某政府學院 `XH/CLMC` 唔同：某市政項目 市政部門 表係**固定 cell 座標**（R8/R11/R15/R22），生成腳本要硬對呢啲座標，唔使搵佔位符。三種模板風格（某大學 `{JBF}` / 某政府學院 `XH` 純大寫 / 某市政項目 市政部門 固定 cell）並存，證明**每項目模板慣例不同，生成前必讀該項目模板提取結構**。

**📌 某市政項目實戰細節**：
- 總表 `材料跟蹤表總表` 150 行，140 項 EL 材料，BQ 章節分佈 ch1(62)/ch4(44)/ch3(13)/ch11(12)/ch2(5)/ch5(2)/ch7(2)
- 品牌分佈：電箱=Legrand/Schneider Electric、燈具=Hoye（主力19項）/皇冠牌/燈下科技、電線=MAS、接地=Uniech/Furse、開關插座=MK/Schneider、梯架線槽=Rovasi/Unitech/WEW、抽氣扇=KDK、乾手機=toto、熱水爐=berlin德國寶
- 申報品牌欄多數寫「與設計相同 / de acordo c/ Projecto」（即跟標書品牌，唔另選）

---

## 📝 全流程步驟（實戰版）

### Step 0：解析任務資料
- 工作區根目錄（用戶俾嘅資料夾）下通常有：`材料審批\`（總表 + 報批表範本）、`材料文件\`（最終 PDF 落咗度）、`工程標書.pdf`（BQ 來源）。
- 用 openpyxl 讀總表，確認 12 欄結構：審批表格編號 / 材料·設備名稱 / BQ專案編號 / 標書品牌 / 數量 / 單位 / 預計貨期·日 / 型號 / 訂貨日期 / 到貨日期 / 審批材料備註及說明 / 備註及說明。

### Step 1：生成分項報批表 xlsx（win32com，預設引擎）
見 `scripts/gen_approval_forms.py`。邏輯：讀總表 → 對每個 item `wsTemplate.Copy` 整張 → 只填值 → 工作表命名 = 編號。
> ⚠️ **BQ 格位係項目特定，唔可以硬編碼！** `gen_approval_forms.py` 嘅 `H5 BQ` 假設**只適用部分模板**。某學校模板真實座標係 **G5:L5（標籤「合約中之項目編號 Item Contratual：」）+ M5:Q5（值）**——若用 H5 會令 BQ 冇顯示、要返辦多次。正確做法見下方「🔧 某學校專案實戰修正」嘅 fingerprint 協議。**生成前必先讀一條 known-good 行確認 BQ 格位，唔好估。**
- 類別勾選格：AR→`I7`、AG/EG→`F8`、EL 排風→`L7`、其餘 EL→`O7`（填 `"■"`）。
- 用戶 VBA 參考：`<REFERENCE_DIR>\bas文件\工程總表宏\Bas\mod_TemplateFiller.bas`。

### Step 2：xlsx → PDF（A4 縱向）
見 `scripts/export_forms_pdf.py`。win32com `ExportAsFixedFormat(0, out)`，設 `ps.Orientation=1`（xlPortrait 縱向）+ `PaperSize=9`（A4）+ `FitToPagesWide/Tall=1` + `Zoom=False`。

### Step 3：附上工程標書 BQ 頁（正確順序！）— 已由 `merge_bq.py` 統一
**頁順序（用戶確認）：`審批表(P1)` → `BQ頁面` → `產品資料(P2..Pn)`**。BQ 插喺審批表之後、產品資料之前（唔係擺最尾）。

見 `scripts/merge_bq.py`（本 v2.0 新嘅統一合併工具，取代舊 `append_bq.py`）。支援兩種 BQ 來源：
- **情況 A：BQ 有文字層** → `--mode auto`，正則掃 BQ PDF 自動建立 `{BQ編號: [頁碼]}`，跨頁項目自動附加全部相關頁。
- **情況 B：BQ 係圖像型掃描件**（好多標書係咁） → 先 `--render-bq <DIR>` 渲染每頁 PNG → 人手 `Read` 讀序號 → 寫 `code2pages.json`（`{"EL-001":[4,5], ...}`）→ `--mode manual --mapping code2pages.json`。

```bash
# 情況 A（有文字層，自動）
python scripts/merge_bq.py \
  --zongbiao 總表.xlsx --bq 工程標書.pdf \
  --input 報批表PDF目錄 --output 合併BQ目錄

# 情況 B（圖像型，先渲染後手動）
python scripts/merge_bq.py --bq 工程標書.pdf --render-bq <TEMP_DIR>/render_bq
# 人手讀 PNG → 寫 mapping.json
python scripts/merge_bq.py \
  --zongbiao 總表.xlsx --bq 工程標書.pdf \
  --input 報批表PDF目錄 --output 合併BQ目錄 \
  --mode manual --mapping mapping.json
```
- 若報批表 PDF 已含產品資料（P1=審批表、P2+=產品），加 `--front-has-product` 令 BQ 插喺中間。
- 若產品資料係獨立 PDF 目錄，加 `--product 產品資料目錄`。
- 輸出目錄會有 `merge_summary.txt` 記低每項成功/跳過原因（含無 BQ 頁項目，方便補 mapping）。
- ⚠️ 跨頁 BQ 項目會自動附加全部相關頁，唔會漏頁。
- ⚠️ 唔好硬抄其他項目嘅序號→頁映射（例如某學校 12 頁=3 樓×4 頁嘅 `[1,5,9]` 映射只適用某學校），每個項目要自己推。

### Step 4：更新總表「型號」列
見 `scripts/extract_models.py`。由材料 PDF 提取型號填入總表第 8 欄：
- **先掃文字層**：PyMuPDF `get_text()` 逐頁掃，有文字嘅 PDF（潔具/機電類）直接 regex 抽型號。
- **圖像型目錄**（水泥/防水/天花/石膏磚/仿石面）：渲染 P5–P8 成 PNG，`Read` 工具肉眼讀產品規格。
- **無材料文件嘅 item**：留空，標註「⚠️ 材料文件夾內無對應 PDF」，等用戶補檔。
- 來源分三級記低：PDF文字精確值 / 材料名規格推導 / 目錄系列名，方便用戶判斷可信度。

### Step 5：品牌欄自動填寫（新增 · 品牌推薦引擎）
標書未指定品牌 → 用 `scripts/brand_recommender.py`（403 條，含實戰補充）推薦，填入文本如「施耐德（法國）或同級」。
```bash
# 單條查詢
cd scripts && python brand_recommender.py      # 互動輸入型號
# 批量填充（標書型號喺第 2 欄 B）
python batch_brand_fill.py 材料清單.xlsx -c 2
python batch_brand_fill.py 材料清單.xlsx --dry-run   # 只預覽
```
**4 級匹配**：① 精確匹配（型號完全一致）→ ② 模糊匹配（關鍵詞重疊>50%）→ ③ 按類別推薦（命中分類）→ ④ 無匹配（填「（待確認）」）。知識庫分 9 類（強電/燈具/弱電/給排水/空調/消防/飾面/門五金/其他）。⚠️ 品牌推薦僅作參考，必須尊重標書原文規定。

---

## 📁 文件產出標準

```
[工程名]\材料審批\
├── 材料審批總表.xlsx              ← Step 4 產出（12 欄）
├── 材料報批表-已填.xlsx           ← Step 1 產出（N 份 Sheet）
├── PDF\                           ← Step 2 產出
│   ├── EL-001 配電箱.pdf
│   └── ...
└── 合併BQ\                        ← Step 3 產出
    ├── EL-001 配電箱（含BQ）.pdf  ← 審批表+BQ+產品資料
    ├── merge_summary.txt
    └── ...
```

| 文件類型 | 命名格式 | 示例 |
|:---|:---|:---|
| 報批總表 | `[工程名]_材料審批總表.xlsx` | `氹仔碼頭_材料審批總表.xlsx` |
| 單份報批表 | `EL-XXX 材料名.pdf` | `EL-001 配電箱.pdf` |
| 合併BQ | `EL-XXX 材料名.pdf`（含 BQ 頁） | `EL-001 配電箱.pdf` |

---

## ⚠️ 用戶已確認嘅硬規則（唔可以違反）

1. **有 logo / 中文字體 / 合併格嘅 Excel 範本 → 絕對唔好用 openpyxl 逐格 clone**。win32com 整張 sheet `.Copy` + 只填細胞值。
2. **A4 縱向 = `PageSetup.Orientation = 1`**（xlPortrait）。轉 PDF 前必顯式設。
3. **批量任務先做 1 份 sample 確認**（P5 規則）：先出 1 份畀用戶確認格式/方向/字體/logo 大細/編號邏輯，OK 先批量。
4. **批量覆寫原檔前必先備份**（去 `<TEMP_DIR>\`）。
5. **用戶手改過嘅 xlsx / docx → 只准讀取學習，禁止擅自 regen 覆寫**。要改局部先用 OfficeCLI。
6. **編號邏輯**：建築 AR-001、機電 EL-001、供水 AG-001、排水 EG-001、冷氣 AC-001。
7. **BQ 頁順序**：審批表 → BQ → 產品資料（中間位，唔係最尾）。
8. **5 步鐵律關卡**：Step 3、Step 5 必須等用戶確認先落手。

---

## 🛑 常見錯誤預防

| 錯誤 | 後果 | 預防措施 |
|:---|:---|:---|
| 跳過 Step 3 直接生成 | 分析錯誤，浪費 Token | 鐵律：每步必須等用戶確認 |
| BQ 編號用區塊標題（(一)(二)） | 匹配失敗 | 檢測標題，自動過濾，取真編號 |
| 含圖片模板用 openpyxl | 圖片丟失、打印設置丟失 | 決策樹自動選 win32com / ZIP |
| BQ PDF 係純掃描圖 | 無法自動匹配頁碼 | `--render-bq` 渲染 → 手寫 mapping → `--mode manual` |
| 硬抄其他項目序號→頁映射 | 頁錯 | 每項目自己推導映射 |
| PowerShell 中文亂碼 | 輸出亂碼 | 腳本已內置 UTF-8 stdout wrapper |
| 模板列寬丟失 | 格式錯亂 | openpyxl 方案已從 XML 讀列寬；win32com 整張 Copy 天然保留 |
| 跨頁 BQ 項目只附一頁 | 資訊不完整 | `merge_bq.py` 自動檢測跨頁項目，附加全部相關頁 |
| 批量錯太多要返工 | 耗時 | P5 規則：先 1 份 sample 確認 |
| D:\ 寫入被 sandbox 擋 | 靜默失敗 | 用 `dangerouslyDisableSandbox: true` 或寫去 `<TEMP_DIR>\` |
| **估 BQ 格位**（H5 vs G5:L5+M5:Q5） | AR-018 BQ 格 flip-flop 6 次、浪費 ~20 分鐘 | **生成前先 fingerprint 一條 known-good 行**，cache 佢嘅 BQ 合併格位，新行直接 copy 嗰個 format，零估計 |
| **win32com 寫 Python datetime** | 日期 off-by-one（naive datetime 當 UTC+8 轉，8/5→變 8/4） | **永遠寫 raw Excel serial**（days since 1899-12-30）。用 `scripts/helpers.py` 嘅 `excel_serial(y,m,d)`，唔好寫 `datetime()` 物件 |
| **重複 render P1 PNG 確認** | 同一份 PDF render 6 次，純粹確認用 | 每份交付物**一次** `fitz.get_text()` 抽 BQ 行做 text check 就夠；只喺異常或用戶要睇才 render 一次 |
| **每個文件寫 bespoke 腳本** | 昨日起咗 6 支散腳本（build / part2 / patch_h5 / patch_bq / fix_ar018 / fix_ar018_p2） | 用**一支 parameterize 嘅 `add_materials.py`** 收 `items.json`，一次過處理 N 個新增/更新碼 |
| **冇 pre-flight 查 sheet 存在** | `sh.Name=code` 報「名稱已使用」要寫 part2 救 | 開 combined workbook 先檢查每個 code 嘅 sheet 是否已存在；存在→更新，唔存在→copy 格式 donor 新建 |

---

## 🔧 某學校專案實戰修正（2026-08-05 自我複查 · 47分鐘→5分鐘目標）

> 2026-08-05 實戰：用戶俾 5 份新 PDF（白鋼管 FUGRO / 填縫劑 Davco / 瓷磚膠 Davco / 信義玻璃），要整合入某學校材料報批包。最後用咗 **47 分鐘、6 支散腳本、AR-018 BQ 格 flip-flop 6 次** 先搞掂。用戶自我複查結論：**核心問題係「靠估 + 試錯」而唔係「讀模板一次 → 重用」**。以下係固化落技能嘅修正，目標下次 5 分鐘。

### ⚠️ 某學校模板真實 cell 座標（gen_approval_forms.py 嘅 H5 假設在此項目係錯嘅！）
| 欄位 | 某學校真實座標 | 錯誤假設 |
|:---|:---|:---|
| 編號 | D5 | — |
| **BQ 編號** | **G5:L5 = 標籤「合約中之項目編號 Item Contratual：」；M5:Q5 = 值**（先 UnMerge G5:R5，再 merge G5:L5 + M5:Q5） | ❌ H5（腳本舊假設） |
| 材料名 | D7 | — |
| 牌子 | D9 | — |
| 數量+單位 | C17（如 `39 直米`） | — |
| 供貨期 | J17 | — |
| 備註/型號 | C19 / K 欄 | — |
| 類別勾選 | AR→`I7`、AG/EG→`F8`、EL排風→`L7`、其餘EL→`O7`（填 `"■"`） | — |

> ⚠️ **唔好硬抄上面座標去其他項目**：每個項目模板唔同（某大學 `{JBF}` / 某政府學院 `XH` / 某市政項目 市政部門 固定 R8/R11/R15/R22 / 某學校 G5:L5+M5:Q5）。**正確做法：生成前先 `fingerprint` 一條 known-good 材料行，cache 佢嘅 BQ 合併格位 + 欄寬 + 數字格式，新行直接 copy 嗰個 format**。一次 introspection，零估計。見 `scripts/helpers.py` 嘅 `set_bq_cell()` / `fingerprint_bq()`。

### 🔑 新增/更新材料碼「一鍵」標準工作流（取代 6 支散腳本）
見 `scripts/add_materials.py`（parameterize，收 `items.json`）。一次過做晒：
1. **Pre-flight**：開 combined workbook，檢查每個 code 嘅 sheet 是否已存在（避免「名稱已使用」報錯 → 要寫 part2）。
2. **設值**：按上面座標填 D5/D7/D9/C17/J17/C19 + BQ 格（G5:L5+M5:Q5）。
3. **出 PDF**：copy 該 sheet 去新 wb → 存 xlsx → `ExportAsFixedFormat(0,...)` A4 縱向。
4. **append 產品資料**：`fitz.insert_pdf()` 插新 PDF 頁（BQ 頁如有亦喺此步）。
5. **sync 總表 + 美化版**（見下）。
> 用一個 json 描述 N 個 item，唔使每個文件寫一支腳本。

### ⚠️ Excel 日期：永遠寫 raw serial，唔寫 Python datetime
win32com 會將 naive datetime 當 UTC+8 轉，令顯示 off-by-one（2026/8/5 → 變 2026/8/4）。總表/美化版「遞交日期」欄一律用 `helpers.excel_serial(2026,8,5)` → `46239.0` 直接寫 cell：
```python
from datetime import date
def excel_serial(y,m,d): return float((date(y,m,d) - date(1899,12,30)).days)
```

### 🎯 驗證紀律（消除 render loop）
- 每份交付物：**一次** `fitz.get_text()` 抽取 BQ 行做 text check 就夠。
- 只在用戶明確要睇，或 text check 異常時，才 render 一次 P1 PNG。
- ❌ 唔好為咗「確認」連續 render 6 次。

### 📋 某學校「美化版」總表（項目根目錄）係標準交付物，要同步
- 路徑：`[工程名]\材料審批總表-..._美化版.xlsx`（project root，唔係 材料文件\）。
- sync 步驟：① 備份；② 喺 `(二)` 機電之前插入新行（AR-018/019 喺 AR-017 後）；③ **copy 隔離行 format**（AR-016=白底→AR-018 白底；AR-017=淺底→AR-019 淺底）保住 zebra；④ EL-003 填 `(二)~(三)` 之間空行；⑤ 日期寫 raw serial；⑥ 保留標題合併 `$A$1:$J$1`。

### 📂 路徑現實（某學校專案）
> ⚠️ 技能上面 Step 0 假設 `材料審批\` 放總表，但**某學校實際**：`材料文件\` 先係真源（含 `材料審批總表-..._已填.xlsx` + `材料報批表-..._已填.xlsx`）。處理某學校時以 `材料文件\` 為準，唔好去 `材料審批\` 搵（會搵唔到 / 舊版）。

---

## 📂 腳本與資源索引

| 用途 | 文件 | 說明 |
|:---|:---|:---|
| 分項報批表 xlsx（win32com） | `scripts/gen_approval_forms.py` | 整張 Copy 範本，只填值，保 logo/字體 |
| xlsx → PDF | `scripts/export_forms_pdf.py` | A4 縱向，COM 導出 |
| **BQ 頁合併（統一）** | `scripts/merge_bq.py` | **v2.3**：v2.2 + 點號BQ(E.1.2.1)/修訂尾綴(-N/_NN/_01)/回復檔(業工程部/顧問單位 reply) |
| 舊項目寫死 BQ 合併（某學校） | `scripts/append_bq.py` | 僅作歷史參考，新任務用 merge_bq.py |
| 型號提取草稿 | `scripts/extract_models.py` | 文字層+圖像型，產型號草稿俾 agent 覆核 |
| **新增/更新材料碼（一鍵）** | `scripts/add_materials.py` | **2026-08-05 自我複查後新增**：收 `items.json`，pre-flight→設值→出PDF→append產品資料→sync總表/美化版，取代 6 支散腳本 |
| **共用 helper** | `scripts/helpers.py` | `excel_serial()`（日期 raw serial，防 off-by-one）、`set_bq_cell()`（某學校 G5:L5+M5:Q5）、`fingerprint_bq()`（讀 known-good 行 cache 格位） |
| 品牌推薦引擎 | `scripts/brand_recommender.py` | 403 條，4 級匹配（case-insensitive） |
| 品牌批量填充 | `scripts/batch_brand_fill.py` | Excel 型號列 → 自動填品牌 |
| 品牌知識庫 | `references/brand_db.json` | 9 類共 403 條（實戰補充後） |
| 品牌示例 | `references/brand_examples.md` | 對話示例 |

---

## 依賴

- Windows + 本機 Excel（win32com 需要）；pywin32 已裝喺 managed venv
- PyMuPDF（fitz）、openpyxl
- venv 路徑：`<WORKBUDDY_DIR>\binaries\python\envs\default\Scripts\python.exe`
- 純數據模板生成可參考 `xlsx-template-batch-fill` 技能

> v2.0 變更：① 加 5 步鐵律確認關卡；② 加引擎選擇決策樹；③ 加編號規則速查（5 大類/EL-ELV/修訂尾綴/電箱組件字母/業主格式）；④ `append_bq.py` 升級為通用 `merge_bq.py`（自動+圖像型+跨頁+產品資料+摘要）；⑤ 併入 162 條品牌推薦引擎，技能自包含。
> v2.1 變更（2026-08-06，某環保項目 實戰補強）：① 品牌庫 +19 條實戰品牌（總 289）；② 編號規則加 BQ前綴=大類 / 修訂尾綴全集 / 合同編號 RCL-DSPA4F / 中文序號前綴（土建XX）/ 申報→批復→雙簽流程；③ `merge_bq.py` 支援總表多 BQ 編號（、分隔）自動拆分全部相關頁。
>
> v2.2 變更（2026-08-06，某大學-某大學一樓裝修工程2025 實戰補強）：① **關鍵修正** BQ前綴係項目/顧問特定（B建築/D機電EME/E電力），必須由總表/BQ PDF讀取，唔可硬編碼；② 新編號格式 AR-xx/EL-0XX-A,B/ELV-00X-A/AC-0001四位零填/EL-0XXB(Ln)燈具B版（含無dash及嵌空格括號變體）；③ 加某大學/酒店雙語模板結構（Ref.No+Item Contratual+勾選框+{JBF}佔位符）同可複製自動鏈（源表格.xlsm→逐編號生成→merge_bq）；④ 品牌庫 +9 條某大學實戰品牌（總 298）。
>
> v2.3 變更（2026-08-06，某政府學院住宿樓2023 實戰補強）：① 業主專屬編號 `3P2023-MTL-{DR/EL/ELV/PL}-[4位]` 完整化（含 3P203 跳號、MST 施工方案前綴、DR 多義警示：某政府學院=排水/某大學某政府項目=建築裝飾）；② BQ 格式加 `E.1.2.1` 點號 + 括號多編號補充「間接包括 X.XXX-X.XXX 項」；③ 修訂尾綴加弱電 `-N`/強電 `_NN`/DR `_01`/`(業工程部 reply)`/`(顧問單位 reply)` 回復標記；④ 審批流程加某政府學院雙簽(■用家+■工務局)及回復檔「原設計 vs 報批」對比結構；⑤ 模板佔位符系統(XH/CLMC/BQXM/SBRQ/MTL/MST 純大寫，不同於某大學 {JBF}；勾選框用私用區字符)；⑥ 品牌庫 +22 條某政府學院實戰品牌（總 320）。
>
> **v2.6.1（2026-08-05→06 自我複查）**：針對某學校 08-05 新增材料任務用咗 47 分鐘、6 支散腳本、AR-018 BQ 格 flip-flop 6 次嘅自我複查修正——① **修正 SKILL 錯誤假設**：`gen_approval_forms.py` 嘅 `H5 BQ` 唔適用某學校，某學校真實 BQ 格 = `G5:L5`（標籤）+ `M5:Q5`（值），已加 ⚠️ 同 fingerprint 協議；② 加「🔧 某學校專案實戰修正」章節（真實 cell 座標 / 一鍵工作流 / 日期 raw serial / 驗證紀律 / 美化版 sync / 路徑現實）；③ 常見錯誤預防表加 5 行（估 BQ 格 / win32com 日期 off-by-one / 重複 render / 散腳本 / 冇 pre-flight）；④ 新增 `scripts/add_materials.py`（一鍵新增/更新材料碼，收 items.json）同 `scripts/helpers.py`（`excel_serial` / `set_bq_cell` / `fingerprint_bq`）。目標：同類任務下次 5 分鐘。
